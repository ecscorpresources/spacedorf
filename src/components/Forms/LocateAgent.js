import React, { Component } from "react";

class LocateAgent extends Component {
  render() {
    return (
      <form>
        <input type="text" placeholder="search for agent" />
      </form>
    );
  }
}

export default LocateAgent;
